<div class="row">
  <div class="wrapper light">
    <div class="row">
        <h2><%= @user.first_name %> <%= @user.last_name %> </h2>
    </div>

    <div class="row">
      <h4>Email</h4>
      <%= @user.email %>
    </div>

    <% if @user.is_a?(User) %>
      <%= render partial: "user_profile" %>
    <% elsif @user.is_a?(Recipient) %>
      <%= redner partial: "recipient_profile" %>
    <% end %>
  </div>
</div>
<%= debug @payments %>
