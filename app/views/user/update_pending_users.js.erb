$('.pending_users').html(
    "<%= escape_javascript(render partial: 'shared/table', locals: { heads: %w[Name Email Status], partials: [['pending_users', @pending_users, :pending_user, 'There are no pending users']] }) %>");
