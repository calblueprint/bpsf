<h1>Create Grant</h1>
<%= form_for @grant do |f| %>
  <%= render 'shared/errors', object: f.object %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.submit 'Create grant!' %>
<% end %>