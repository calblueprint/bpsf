<div class="row">
  <div class="twelve columns">
    <div class="wrapper light">

      <div class="row">
        <h2>Admin Dashboard</h2>

        <table>
          <thead>
            <tr>
              <th >Name</th>
              <th >Status</th>
              <th >Set Status</th>
            </tr>
          </thead>
          <tbody id="grantlisting">
            <%= render partial: 'all_grants', collection: @grants, as: :grant %>
          </tbody>
        </table>
      </div>

    </div>
  </div>
</div>
<%= render partial: "crowdfund_modal" %>
<p class="btn primary medium"><a href="#" class="switch" gumby-trigger="#modal1">Open Modal</a></p>
