<!--*****************************************MENU**********************************************-->
  <div class="row">
    <div class="twelve columns">
      <span class="category pull_left">Category:&emsp;</span>
      <li class="field metro inlined">
        <div class="picker">
          <%= form_tag(:action => :home, :id => 'filter_subject') do %>
            <%= select_tag "subject", options_for_select(PagesController::MENU, params[:subject]), :onchange => "this.form.submit()" %>
          <% end %>
        </div>
      </li>
      <span class="dayleft pull_right">Days Left: 17</span>
    </div>
  </div>
